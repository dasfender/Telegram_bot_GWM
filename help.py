from aiogram import Router, F, types

router = Router()

@router.message(F.text == "/help")
async def help_command(message: types.Message):
    help_text = (
        "📋 **Инструкция по отправке заявки**\n\n"
        "1️⃣ **Описание проблемы** — начните с подробного описания неисправности.\n"
        "   - Укажите симптомы, условия при которых проявляется проблема.\n\n"
        "2️⃣ **Фото VIN и пробега** — сделайте чёткое фото VIN-номера и пробега на одометре.\n\n"
        "3️⃣ **Фото дефекта** — добавьте фото места или детали, где проявляется проблема.\n\n"
        "4️⃣ **Видео (по возможности)** — снимите короткое видео, демонстрирующее проблему.\n\n"
        "📌 *Один запрос — это один случай обращения.* После завершения запроса материалы отправляются администратору и архивируются.\n\n"
        "🚫 После завершения запроса добавить материалы нельзя — только создать новый.\n\n"
        "💡 Если у вас возникли трудности — обратитесь к администратору."
    )
    await message.answer(help_text, parse_mode="Markdown")